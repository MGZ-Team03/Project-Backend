# WebSocket 아키텍처 정의

## 📋 개요

**목표**: SpeakTracker 프로젝트의 실시간 통신을 위한 WebSocket 아키텍처 정의  
**범위**: 연결, 상태 전송, 피드백, 대시보드 브로드캐스트  
**기반 기술**: AWS API Gateway WebSocket API, DynamoDB

---

## 1. 메시지 타입 정의

### 1.1 Connection (연결)

**목적**: 사용자가 WebSocket에 연결할 때 세션 정보 저장

```json
{
  "type": "connection",
  "action": "connect",
  "user_email": "student@example.com",
  "user_type": "student|tutor",
  "timestamp": "2026-01-14T10:30:00Z"
}
```

**처리 흐름**:
1. 클라이언트가 WebSocket 연결 ($connect)
2. API Gateway가 `connection_id` 자동 생성
3. WebSocketConnectionsTable에 저장:
   - connection_id (PK)
   - user_email
   - user_type
   - tutor_email (학생인 경우, Tutor-Students 테이블 조회)
   - connected_at
   - ttl (24시간)

**응답**:
```json
{
  "type": "connection_success",
  "connection_id": "abc123",
  "timestamp": "2026-01-14T10:30:00Z"
}
```

---

### 1.2 Status (상태 전송)

**목적**: 학생의 현재 학습 상태를 튜터에게 실시간 전달

```json
{
  "type": "status",
  "user_email": "student@example.com",
  "status": "studying|idle|paused|finished",
  "session_id": "session_123",
  "current_sentence": "Hello, how are you?",
  "progress": 45,
  "timestamp": "2026-01-14T10:30:00Z"
}
```

**상태 정의**:
| 상태 | 설명 | 어디서 발생 |
|------|------|-----------|
| `studying` | 문장 연습 중 | 학생 화면 |
| `idle` | 학습 중이지만 입력 없음 | 일정 시간 입력 없을 때 |
| `paused` | 연습 일시 중지 | 학생이 멈춤 버튼 클릭 |
| `finished` | 세션 종료 | 학생이 종료 |

**처리 흐름**:
1. 학생 클라이언트가 상태 변경 시 메시지 전송
2. WebSocket 핸들러가 수신
3. (선택) SessionsTable에 상태 기록
4. **대시보드 브로드캐스트**: 담당 튜터에게 상태 전송

---

### 1.3 Feedback (튜터 피드백)

**목적**: 튜터가 학생에게 실시간 피드백 전송

```json
{
  "type": "feedback",
  "from": "tutor@example.com",
  "student_email": "student@example.com",
  "session_id": "session_123",
  "message": "발음이 좋아졌어요",
  "message_type": "text|tts",
  "audio_url": "https://s3.amazonaws.com/bucket/feedback.mp3",
  "timestamp": "2026-01-14T10:30:00Z"
}
```

**메시지 타입**:
| 타입 | 설명 | 포함 필드 |
|------|------|----------|
| `text` | 텍스트 피드백 | message |
| `tts` | 음성 피드백 | message, audio_url |

**처리 흐름**:
1. 튜터가 REST API `/api/tutor/feedback`로 피드백 전송
2. TutorFeedbackHandler가 수신
3. FeedbackMessagesTable에 저장
4. **WebSocket 전송**: 학생의 connection_id로 메시지 전송
5. 학생 클라이언트가 수신 후 UI 업데이트

---

### 1.4 Dashboard (대시보드 브로드캐스트)

**목적**: 튜터 대시보드에 담당 학생들의 상태를 1:N으로 브로드캐스트

```json
{
  "type": "dashboard",
  "action": "update",
  "target_tutor_email": "tutor@example.com",
  "data": {
    "student_email": "student@example.com",
    "status": "studying",
    "current_sentence": "Hello, how are you?",
    "progress": 45,
    "last_update": "2026-01-14T10:30:00Z"
  }
}
```

**브로드캐스트 타입**:
| Action | 설명 | 언제 발생 |
|--------|------|----------|
| `update` | 학생 상태 업데이트 | 상태 변경 시 |
| `student_joined` | 학생 세션 시작 | 학생이 공부 시작 |
| `student_left` | 학생 세션 종료 | 학생이 공부 종료 |
| `broadcast_all` | 모든 학생 현황 | 튜터가 대시보드 접속 |

**처리 흐름**:
1. 학생이 상태 변경 메시지 전송
2. WebSocket 핸들러가 상태 수신
3. WebSocketConnectionsTable에서 `tutor_email`으로 모든 담당 학생 조회 (GSI)
4. 각 학생의 connection_id로 대시보드 업데이트 메시지 전송

---

## 2. WebSocket 라우팅 규칙

### 2.1 1:1 메시지 (학생 ← → 튜터)

#### 피드백 (Tutor → Student)
```
Tutor (REST API)
    ↓
TutorFeedbackHandler
    ↓
FeedbackMessagesTable 저장
    ↓
WebSocketConnectionsTable 조회 (student_email)
    ↓
API Gateway Management API
    ↓
Student (WebSocket)
```

**구현 순서**:
1. `student_email`으로 WebSocketConnectionsTable 조회
2. 해당 학생의 모든 `connection_id` 조회
3. 각 connection_id로 메시지 전송

---

### 2.2 1:N 메시지 (튜터 ← 학생들, 대시보드)

#### 상태 전송 (Student → Tutor Dashboard)
```
Student (상태 변경)
    ↓
WebSocket 메시지 (Status)
    ↓
WebSocket 핸들러
    ↓
WebSocketConnectionsTable 조회 (tutor_email GSI)
    ↓
모든 담당 학생의 connection_id
    ↓
Tutor (대시보드 실시간 업데이트)
```

**구현 순서**:
1. 학생 상태 메시지 수신
2. 학생의 이메일로 Tutor-Students 테이블 조회 (담당 튜터 찾기)
3. WebSocketConnectionsTable에서 `tutor_email` GSI 조회
4. 튜터의 모든 connection_id 조회
5. 각 connection_id로 대시보드 업데이트 메시지 전송

---

### 2.3 연결 해제 처리

```
Client 연결 해제 또는 TTL 만료
    ↓
$disconnect 이벤트
    ↓
WebSocketConnectionsTable에서 connection_id 삭제
    ↓
관련 메시지 스트림 종료
```

---

## 3. DynamoDB 테이블 설계

### 3.1 WebSocketConnectionsTable

**테이블명**: `${AWS::StackName}-websocket-connections`

| 속성 | 타입 | 키 | 설명 |
|------|------|-----|------|
| `connection_id` | String | **PK (HASH)** | API Gateway 자동 생성 ID |
| `user_email` | String | GSI-1 | 사용자 이메일 (학생/튜터) |
| `tutor_email` | String | GSI-2 | 담당 튜터 이메일 (학생의 경우만) |
| `user_type` | String | - | "student" 또는 "tutor" |
| `connected_at` | String | - | 연결 시각 (ISO 8601) |
| `ttl` | Number | - | TTL 타임스탬프 (24시간) |

**GSI 정의**:
| 인덱스명 | PK | SK | 용도 |
|---------|-----|-----|------|
| `user_email-index` | user_email | - | 사용자의 모든 connection 조회 |
| `tutor_email-index` | tutor_email | - | 튜터의 모든 학생 connection 조회 |

**예시 데이터**:
```json
{
  "connection_id": "abc123def456",
  "user_email": "student@example.com",
  "tutor_email": "tutor@example.com",
  "user_type": "student",
  "connected_at": "2026-01-14T10:30:00Z",
  "ttl": 1705272600
}
```

---

### 3.2 FeedbackMessagesTable

**테이블명**: `${AWS::StackName}-feedback-messages`

| 속성 | 타입 | 키 | 설명 |
|------|------|-----|------|
| `composite_key` | String | **PK (HASH)** | `${tutor_email}#${student_email}#${session_id}` |
| `timestamp` | String | **SK (RANGE)** | 메시지 발송 시각 (ISO 8601) |
| `student_email` | String | GSI-1 | 학생 이메일 |
| `tutor_email` | String | - | 튜터 이메일 |
| `message_text` | String | - | 피드백 메시지 |
| `message_type` | String | - | "text" 또는 "tts" |
| `audio_url` | String | - | TTS 오디오 S3 URL (message_type="tts"인 경우) |
| `ttl` | Number | - | TTL 타임스탬프 (30일) |

**예시 데이터**:
```json
{
  "composite_key": "tutor@example.com#student@example.com#session_123",
  "timestamp": "2026-01-14T10:30:00Z",
  "student_email": "student@example.com",
  "tutor_email": "tutor@example.com",
  "message_text": "발음이 좋아졌어요",
  "message_type": "text",
  "ttl": 1708470600
}
```

---

## 4. Connection 생명주기

### 생성 (Connect)

```
1. 클라이언트 → WebSocket 연결
   ws://api.example.com/dev

2. AWS API Gateway
   → $connect 라우트 트리거
   → Lambda 핸들러 호출

3. Lambda 핸들러
   - connection_id 자동 생성
   - user_email, user_type 추출 (쿼리 파라미터 또는 헤더)
   - user_type이 "student"인 경우:
     * Tutor-Students 테이블 조회
     * tutor_email 추출
   - WebSocketConnectionsTable에 저장

4. 클라이언트
   ← connection_success 응답
```

### 메시지 송수신 (Default)

```
1. 클라이언트 → WebSocket 메시지
   {
     "type": "status|feedback|...",
     ...
   }

2. AWS API Gateway
   → $default 라우트 트리거
   → Lambda 핸들러 호출

3. Lambda 핸들러
   - 메시지 타입에 따라 처리
   - 필요 시 DynamoDB에 저장
   - 필요 시 다른 connection_id로 포워드

4. API Gateway Management API
   - 수신자 connection_id로 메시지 전송

5. 수신자 클라이언트
   ← 메시지 수신 → UI 업데이트
```

### 종료 (Disconnect)

```
1. 클라이언트
   - 연결 해제 (close 호출)
   - 또는 TTL 만료

2. AWS API Gateway
   → $disconnect 라우트 트리거
   → Lambda 핸들러 호출

3. Lambda 핸들러
   - connection_id로 WebSocketConnectionsTable 조회
   - DynamoDB에서 삭제

4. 리소스 정리 완료
```

---

## 5. 에러 처리

### 5.1 Connection 실패

```json
{
  "type": "connection_error",
  "code": "INVALID_CREDENTIALS|TUTOR_NOT_FOUND",
  "message": "사용자 정보를 확인할 수 없습니다",
  "timestamp": "2026-01-14T10:30:00Z"
}
```

### 5.2 메시지 전송 실패

```json
{
  "type": "message_error",
  "code": "RECIPIENT_OFFLINE|DELIVERY_FAILED",
  "original_message_id": "msg_123",
  "message": "메시지 전송에 실패했습니다",
  "timestamp": "2026-01-14T10:30:00Z"
}
```

### 5.3 타임아웃

```json
{
  "type": "timeout",
  "code": "IDLE_TIMEOUT",
  "message": "연결이 30분 동안 활동이 없어 종료되었습니다",
  "timestamp": "2026-01-14T10:30:00Z"
}
```

---

## 6. 구현 체크리스트

### 필수 구현

- [ ] WebSocket $connect 핸들러
  - connection_id 저장
  - user_email, user_type 추출
  - tutor_email 조회 및 저장 (학생인 경우)

- [ ] WebSocket $default 핸들러
  - 메시지 타입 분류
  - 각 타입별 처리 로직

- [ ] WebSocket $disconnect 핸들러
  - connection 삭제

- [ ] 1:1 메시지 라우팅 (피드백)
  - FeedbackMessagesTable 저장
  - student_email으로 connection 조회
  - API Gateway Management API로 전송

- [ ] 1:N 메시지 라우팅 (대시보드)
  - tutor_email으로 모든 학생 connection 조회 (GSI)
  - 일괄 메시지 전송

### 선택 구현

- [ ] 재시도 로직 (메시지 전송 실패)
- [ ] 메시지 압축
- [ ] 메시지 암호화
- [ ] 연결 풀 관리

---

## 7. 성능 최적화

### 7.1 DynamoDB 쿼리 최적화

| 쿼리 | 최적화 방법 | 예상 성능 |
|------|-----------|---------|
| `student_email` 조회 | GSI 사용 | O(1) |
| `tutor_email` 조회 | GSI 사용 (배치) | O(학생수) |
| connection 삭제 | 키로 직접 삭제 | O(1) |

### 7.2 API Gateway Management API 최적화

```
배치 전송 (권장):
- 100개 이상의 connection_id가 있을 때
- 병렬 전송 (동시 10개 정도)
- 실패한 것만 재시도
```

### 7.3 메모리 관리

```
- Connection 정보는 DynamoDB만 사용 (메모리 캐싱 X)
- 메시지는 DynamoDB에 저장 후 폐기
- 임시 버퍼는 최소화
```

---

## 8. 보안 고려사항

### 8.1 인증/인가

```
- 모든 WebSocket 연결 시 user_email 검증
- JWT 토큰 또는 Cognito 세션 확인
- user_type에 따른 권한 검증
```

### 8.2 메시지 검증

```
- 메시지 크기 제한 (예: 5KB)
- 메시지 타입 화이트리스트
- SQL 인젝션/XSS 방지 (입력값 sanitize)
```

### 8.3 레이트 제한

```
- 사용자당 메시지 전송 제한 (예: 초당 10개)
- 연결당 데이터 전송 제한 (예: 분당 100KB)
```

---

## 9. 모니터링 및 로깅

### 로그 항목

```json
{
  "timestamp": "2026-01-14T10:30:00Z",
  "event_type": "connection|message|error",
  "connection_id": "abc123",
  "user_email": "student@example.com",
  "message_type": "status|feedback",
  "status": "success|error",
  "duration_ms": 150
}
```

### 모니터링 메트릭

| 메트릭 | 알람 기준 |
|--------|---------|
| 활성 연결 수 | > 1000 |
| 메시지 지연 | > 5초 |
| 에러율 | > 1% |
| TTL 삭제율 | > 10% |

---

## 10. 참고 자료

- [AWS API Gateway WebSocket API](https://docs.aws.amazon.com/apigateway/latest/developerguide/websocket-api.html)
- [AWS API Gateway Management API](https://docs.aws.amazon.com/apigatewaymanagementapi/)
- [DynamoDB 성능 최적화](https://docs.aws.amazon.com/amazondynamodb/latest/developerguide/best-practices.html)
- [AWS Lambda 타임아웃](https://docs.aws.amazon.com/lambda/latest/dg/limits.html)


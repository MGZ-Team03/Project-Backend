# 튜터 등록 기능 구현 로드맵

## 개요

학생이 튜터를 검색하고 등록 요청을 보내면, 튜터가 승인/거부할 수 있는 기능을 구현합니다.

---

## 핵심 플로우

```
[학생]
  ↓ POST /api/tutors/{email}/request
  
[Lambda]
  ↓ tutor_requests 테이블에 status='pending' 저장
  ↓ 튜터에게 WebSocket 알림
  
[튜터]
  ↓ GET /api/tutor/requests (pending 요청 조회)
  ↓ POST /api/tutors/requests/{id}/approve
  
[Lambda]
  ↓ tutor_requests → status='approved'
  ↓ tutor_students 테이블에 관계 추가
  ↓ 학생에게 승인 알림
  
[완료]
```

### 거부 플로우

```
[튜터]
  ↓ POST /api/tutors/requests/{id}/reject
  
[Lambda]
  ↓ tutor_requests → status='rejected'
  ↓ rejection_reason 저장
  ↓ 학생에게 거부 알림
  
[완료]
```

---

## 데이터베이스 설계

### 1. tutor_requests (신규 테이블)

**용도**: 튜터 등록 요청 관리 및 이력 추적

```
PK: request_id (UUID)
SK: created_at (Timestamp)

Attributes:
- student_email: String
- tutor_email: String
- status: String ('pending' | 'approved' | 'rejected' | 'cancelled')
- message: String (optional, 학생의 요청 메시지)
- processed_at: DateTime (optional, 승인/거부 처리 시각)
- rejection_reason: String (optional, 거부 사유)
- created_at: DateTime
- updated_at: DateTime

GSI1: student_email-created_at-index
  PK: student_email
  SK: created_at
  용도: 학생이 자신의 모든 요청 조회

GSI2: tutor_email-status-created_at-index
  PK: tutor_email#status
  SK: created_at
  용도: 튜터가 status별(pending/approved/rejected) 요청 조회

TTL: 90일 (처리 완료 후 90일 자동 삭제)
```

### 2. tutor_students (기존 테이블 - 승인된 관계만 저장)

```
PK: tutor_email
SK: student_email

Attributes:
- assigned_at: DateTime
- status: String ('active' | 'inactive')
- request_id: String (참조용, tutor_requests의 request_id)

GSI: student_email-index
  용도: 학생의 현재 튜터 조회
```

### 3. users (기존 테이블)

```
PK: email

Attributes:
- name: String
- role: String ('student' | 'tutor')
- profile_image: String (optional)
- bio: String (optional, 튜터 소개)
- specialties: List<String> (optional, 튜터 전문 분야)
- max_students: Number (optional, 튜터 최대 학생 수)
- is_accepting: Boolean (optional, 튜터가 현재 학생 모집 중인지)
- created_at: DateTime
```

---

## API 설계

### 1. 튜터 목록 조회 (학생용)

```http
GET /api/tutors?search={keyword}&specialty={specialty}
Authorization: Bearer {token}
```

**Query Parameters:**
- `search` (optional): 튜터 이름 검색
- `specialty` (optional): 전문 분야 필터 (발음, 문법, 회화 등)

**Response:**
```json
{
  "tutors": [
    {
      "email": "tutor1@example.com",
      "name": "이영어",
      "bio": "10년 경력 영어 발음 전문 튜터",
      "specialties": ["발음", "회화"],
      "profile_image": "https://...",
      "current_students": 8,
      "max_students": 15,
      "is_accepting": true,
      "my_request_status": null // 또는 "pending", "approved", "rejected"
    },
    {
      "email": "tutor2@example.com",
      "name": "박발음",
      "bio": "미국 거주 10년 원어민 수준 발음 교정",
      "specialties": ["발음"],
      "profile_image": "https://...",
      "current_students": 12,
      "max_students": 10,
      "is_accepting": false,
      "my_request_status": "pending"
    }
  ]
}
```

### 2. 튜터 등록 요청 (학생용)

```http
POST /api/tutors/{tutor_email}/request
Authorization: Bearer {token}
Content-Type: application/json
```

**Request Body:**
```json
{
  "message": "발음 교정을 받고 싶어서 등록 요청드립니다."
}
```

**Response:**
```json
{
  "request_id": "req_abc123",
  "tutor_email": "tutor1@example.com",
  "status": "pending",
  "created_at": "2026-01-20T10:30:00Z",
  "message": "등록 요청이 전송되었습니다."
}
```

**Lambda 처리 로직:**
1. 유효성 검사 (중복 요청, 이미 등록된 학생, 튜터 정원 확인)
2. tutor_requests 테이블에 status='pending'으로 저장
3. 튜터에게 WebSocket 알림 전송 (NEW_TUTOR_REQUEST)

**Error Cases:**
- 400: 이미 해당 튜터에게 pending 요청이 있음
- 400: 이미 해당 튜터의 학생임
- 400: 튜터가 학생을 받지 않음 (is_accepting: false)
- 400: 튜터의 정원 초과
- 404: 튜터를 찾을 수 없음

### 3. 내 요청 목록 조회 (학생용)

```http
GET /api/my/tutor-requests?status={status}
Authorization: Bearer {token}
```

**Query Parameters:**
- `status` (optional): pending, approved, rejected, all (default: all)

**Response:**
```json
{
  "requests": [
    {
      "request_id": "req_abc123",
      "tutor_email": "tutor1@example.com",
      "tutor_name": "이영어",
      "status": "pending",
      "message": "발음 교정을 받고 싶어서 등록 요청드립니다.",
      "created_at": "2026-01-20T10:30:00Z",
      "processed_at": null,
      "rejection_reason": null
    },
    {
      "request_id": "req_def456",
      "tutor_email": "tutor2@example.com",
      "tutor_name": "박발음",
      "status": "approved",
      "message": "잘 부탁드립니다!",
      "created_at": "2026-01-18T14:20:00Z",
      "processed_at": "2026-01-18T15:00:00Z",
      "rejection_reason": null
    }
  ]
}
```

### 4. 대기 중인 등록 요청 조회 (튜터용)

```http
GET /api/tutor/requests?status={status}
Authorization: Bearer {token}
```

**Query Parameters:**
- `status` (optional): pending, approved, rejected, all (default: pending)

**Response:**
```json
{
  "requests": [
    {
      "request_id": "req_abc123",
      "student_email": "student1@example.com",
      "student_name": "김영희",
      "status": "pending",
      "message": "발음 교정을 받고 싶어서 등록 요청드립니다.",
      "created_at": "2026-01-20T10:30:00Z",
      "days_waiting": 1
    },
    {
      "request_id": "req_ghi789",
      "student_email": "student2@example.com",
      "student_name": "박철수",
      "status": "pending",
      "message": "회화 실력 향상을 위해 등록 요청합니다.",
      "created_at": "2026-01-19T09:15:00Z",
      "days_waiting": 2
    }
  ],
  "total_pending": 2
}
```

### 5. 등록 요청 승인 (튜터용)

```http
POST /api/tutors/requests/{request_id}/approve
Authorization: Bearer {token}
```

**Response:**
```json
{
  "request_id": "req_abc123",
  "status": "approved",
  "student_email": "student1@example.com",
  "processed_at": "2026-01-20T11:00:00Z",
  "message": "학생이 등록되었습니다."
}
```

**Lambda 처리 로직:**
1. 요청 유효성 검사 (이미 처리됨, 정원 확인, 권한 확인)
2. tutor_requests 테이블 업데이트 (status: 'approved', processed_at 설정)
3. tutor_students 테이블에 관계 추가 (DynamoDB 트랜잭션 사용)
4. 학생에게 WebSocket 알림 전송 (TUTOR_REQUEST_APPROVED)

**Error Cases:**
- 400: 이미 처리된 요청
- 400: 튜터의 정원이 가득 참
- 403: 본인의 요청이 아님
- 404: 요청을 찾을 수 없음

### 6. 등록 요청 거부 (튜터용)

```http
POST /api/tutors/requests/{request_id}/reject
Authorization: Bearer {token}
Content-Type: application/json
```

**Request Body:**
```json
{
  "reason": "현재 정원이 가득 차서 더 이상 학생을 받을 수 없습니다."
}
```

**Response:**
```json
{
  "request_id": "req_abc123",
  "status": "rejected",
  "student_email": "student1@example.com",
  "processed_at": "2026-01-20T11:00:00Z",
  "rejection_reason": "현재 정원이 가득 차서 더 이상 학생을 받을 수 없습니다.",
  "message": "요청이 거부되었습니다."
}
```

**Lambda 처리 로직:**
1. 요청 유효성 검사 (이미 처리됨, 권한 확인)
2. tutor_requests 테이블 업데이트 (status: 'rejected', processed_at, rejection_reason 설정)
3. 학생에게 WebSocket 알림 전송 (TUTOR_REQUEST_REJECTED)

### 7. 요청 취소 (학생용)

```http
DELETE /api/my/tutor-requests/{request_id}
Authorization: Bearer {token}
```

**Response:**
```json
{
  "request_id": "req_abc123",
  "status": "cancelled",
  "message": "요청이 취소되었습니다."
}
```

**조건:**
- pending 상태일 때만 취소 가능
- 이미 승인/거부된 요청은 취소 불가

---

## Lambda Functions 설계

### 1. TutorRegisterFunction (신규)

**역할**: 튜터 등록 관련 모든 API 처리

**엔드포인트:**
- `GET /api/tutors` - 튜터 목록 조회
- `POST /api/tutors/{email}/request` - 등록 요청
- `GET /api/my/tutor-requests` - 학생의 요청 목록
- `DELETE /api/my/tutor-requests/{id}` - 요청 취소
- `GET /api/tutor/requests` - 튜터의 요청 목록
- `POST /api/tutors/requests/{id}/approve` - 승인
- `POST /api/tutors/requests/{id}/reject` - 거부

**Dependencies:**
```gradle
dependencies {
    implementation 'com.amazonaws:aws-lambda-java-core:1.2.2'
    implementation 'com.amazonaws:aws-lambda-java-events:3.11.0'
    implementation 'software.amazon.awssdk:dynamodb:2.20.0'
    implementation 'software.amazon.awssdk:apigatewaymanagementapi:2.20.0' // WebSocket
    implementation 'com.google.code.gson:gson:2.10.1'
    implementation 'java.util.UUID'
}
```

**환경 변수:**
- `USERS_TABLE`: users 테이블 이름
- `TUTOR_REQUESTS_TABLE`: tutor_requests 테이블 이름
- `TUTOR_STUDENTS_TABLE`: tutor_students 테이블 이름
- `WEBSOCKET_CONNECTIONS_TABLE`: websocket_connections 테이블 이름
- `WEBSOCKET_API_ENDPOINT`: WebSocket API 엔드포인트

### 2. WebSocket 알림 처리

**알림 타입:**

1. **튜터에게 알림 (새 요청)**
```json
{
  "type": "NEW_TUTOR_REQUEST",
  "data": {
    "request_id": "req_abc123",
    "student_email": "student1@example.com",
    "student_name": "김영희",
    "message": "발음 교정을 받고 싶어서 등록 요청드립니다.",
    "created_at": "2026-01-20T10:30:00Z"
  }
}
```

2. **학생에게 알림 (승인)**
```json
{
  "type": "TUTOR_REQUEST_APPROVED",
  "data": {
    "request_id": "req_abc123",
    "tutor_email": "tutor1@example.com",
    "tutor_name": "이영어",
    "processed_at": "2026-01-20T11:00:00Z"
  }
}
```

3. **학생에게 알림 (거부)**
```json
{
  "type": "TUTOR_REQUEST_REJECTED",
  "data": {
    "request_id": "req_abc123",
    "tutor_email": "tutor1@example.com",
    "tutor_name": "이영어",
    "rejection_reason": "현재 정원이 가득 차서 더 이상 학생을 받을 수 없습니다.",
    "processed_at": "2026-01-20T11:00:00Z"
  }
}
```

---

## 비즈니스 로직

### 1. 중복 요청 방지

```java
// 학생이 같은 튜터에게 pending 요청이 이미 있는지 확인
QueryRequest queryRequest = QueryRequest.builder()
    .tableName(TUTOR_REQUESTS_TABLE)
    .indexName("student_email-created_at-index")
    .keyConditionExpression("student_email = :email")
    .filterExpression("tutor_email = :tutor AND #status = :pending")
    .expressionAttributeNames(Map.of("#status", "status"))
    .expressionAttributeValues(Map.of(
        ":email", AttributeValue.builder().s(studentEmail).build(),
        ":tutor", AttributeValue.builder().s(tutorEmail).build(),
        ":pending", AttributeValue.builder().s("pending").build()
    ))
    .build();

// 결과가 있으면 400 에러 반환
```

### 2. 이미 등록된 학생인지 확인

```java
// tutor_students 테이블에서 관계 확인
GetItemRequest getItemRequest = GetItemRequest.builder()
    .tableName(TUTOR_STUDENTS_TABLE)
    .key(Map.of(
        "tutor_email", AttributeValue.builder().s(tutorEmail).build(),
        "student_email", AttributeValue.builder().s(studentEmail).build()
    ))
    .build();

// 이미 관계가 있으면 400 에러 반환
```

### 3. 튜터 정원 확인

```java
// 튜터의 현재 학생 수 조회
QueryRequest queryRequest = QueryRequest.builder()
    .tableName(TUTOR_STUDENTS_TABLE)
    .keyConditionExpression("tutor_email = :email")
    .filterExpression("#status = :active")
    .expressionAttributeNames(Map.of("#status", "status"))
    .expressionAttributeValues(Map.of(
        ":email", AttributeValue.builder().s(tutorEmail).build(),
        ":active", AttributeValue.builder().s("active").build()
    ))
    .select(Select.COUNT)
    .build();

int currentStudents = response.count();

// users 테이블에서 max_students 조회
GetItemRequest getUserRequest = GetItemRequest.builder()
    .tableName(USERS_TABLE)
    .key(Map.of("email", AttributeValue.builder().s(tutorEmail).build()))
    .build();

int maxStudents = Integer.parseInt(
    response.item().get("max_students").n()
);

if (currentStudents >= maxStudents) {
    // 400 에러: 정원 초과
}
```

### 4. 자동 만료 (선택사항)

Lambda + EventBridge를 사용하여 매일 실행:
- 7일 이상 pending 상태인 요청을 자동 취소
- 학생에게 만료 알림

```java
// Scan으로 7일 이상 pending 요청 찾기
long sevenDaysAgo = Instant.now().minus(7, ChronoUnit.DAYS)
    .toEpochMilli();

ScanRequest scanRequest = ScanRequest.builder()
    .tableName(TUTOR_REQUESTS_TABLE)
    .filterExpression("#status = :pending AND created_at < :threshold")
    .expressionAttributeNames(Map.of("#status", "status"))
    .expressionAttributeValues(Map.of(
        ":pending", AttributeValue.builder().s("pending").build(),
        ":threshold", AttributeValue.builder().n(String.valueOf(sevenDaysAgo)).build()
    ))
    .build();

// 각 요청을 'cancelled' 상태로 업데이트
```

---

## 구현 단계

### Phase 1: 기본 인프라 구축 (1-2일)

- [ ] DynamoDB 테이블 생성
  - [ ] tutor_requests 테이블
  - [ ] GSI 설정 (student_email-index, tutor_email-status-index)
- [ ] users 테이블에 튜터 관련 필드 추가
  - [ ] bio, specialties, max_students, is_accepting
- [ ] TutorRegisterFunction Lambda 생성
  - [ ] build.gradle 설정
  - [ ] 핸들러 클래스 구조 생성

### Phase 2: 학생 기능 구현 (2-3일)

- [ ] 튜터 목록 조회 API
  - [ ] GET /api/tutors
  - [ ] 검색 및 필터링 로직
  - [ ] my_request_status 포함
- [ ] 등록 요청 API
  - [ ] POST /api/tutors/{email}/request
  - [ ] Lambda: 유효성 검사 (중복, 정원, is_accepting)
  - [ ] Lambda: tutor_requests 테이블에 status='pending' 저장
  - [ ] Lambda: 튜터에게 WebSocket 알림 전송
- [ ] 내 요청 목록 조회 API
  - [ ] GET /api/my/tutor-requests
  - [ ] 상태별 필터링
- [ ] 요청 취소 API
  - [ ] DELETE /api/my/tutor-requests/{id}

### Phase 3: 튜터 기능 구현 (2-3일)

- [ ] 대기 중인 요청 조회 API
  - [ ] GET /api/tutor/requests
  - [ ] GSI로 pending 요청 리스트 조회
  - [ ] 대기 일수 계산
- [ ] 승인 API
  - [ ] POST /api/tutors/requests/{id}/approve
  - [ ] Lambda: tutor_requests → status='approved' 업데이트
  - [ ] Lambda: tutor_students 테이블에 관계 추가
  - [ ] Lambda: 트랜잭션 처리 (DynamoDB TransactWriteItems)
  - [ ] Lambda: 학생에게 WebSocket 알림 전송
- [ ] 거부 API
  - [ ] POST /api/tutors/requests/{id}/reject
  - [ ] Lambda: tutor_requests → status='rejected' 업데이트
  - [ ] Lambda: rejection_reason 저장
  - [ ] Lambda: 학생에게 WebSocket 알림 전송

### Phase 4: WebSocket 알림 연동 (2일)

- [ ] WebSocket 알림 헬퍼 클래스 작성
- [ ] 새 요청 시 튜터에게 알림
- [ ] 승인 시 학생에게 알림
- [ ] 거부 시 학생에게 알림
- [ ] 연결 끊김 처리 (fallback)

### Phase 5: API Gateway 연동 (1일)

- [ ] template.yaml에 API 엔드포인트 추가
- [ ] CORS 설정
- [ ] Cognito 인증 연동
- [ ] 권한 검증 (학생/튜터 role)

### Phase 6: 테스트 (2일)

- [ ] 단위 테스트
  - [ ] 유효성 검사 로직
  - [ ] 비즈니스 로직
- [ ] 통합 테스트
  - [ ] API 엔드포인트 테스트
  - [ ] DynamoDB 작업 테스트
- [ ] 시나리오 테스트
  - [ ] 정상 플로우 (요청 → 승인)
  - [ ] 정상 플로우 (요청 → 거부)
  - [ ] 에러 케이스 (중복, 정원 초과 등)

### Phase 7: 추가 기능 (선택사항, 1-2일)

- [ ] 자동 만료 Lambda (EventBridge)
- [ ] 이메일 알림 (SES)
- [ ] 튜터 평점 시스템
- [ ] 통계 (승인률, 평균 응답 시간)

---

## template.yaml 추가 사항

```yaml
TutorRegisterFunction:
  Type: AWS::Serverless::Function
  Properties:
    CodeUri: TutorRegisterFunction
    Handler: tutorregister.App::handleRequest
    Runtime: java17
    Architectures:
      - x86_64
    MemorySize: 512
    Timeout: 30
    Environment:
      Variables:
        USERS_TABLE: !Ref UsersTable
        TUTOR_REQUESTS_TABLE: !Ref TutorRequestsTable
        TUTOR_STUDENTS_TABLE: !Ref TutorStudentsTable
        WEBSOCKET_CONNECTIONS_TABLE: !Ref WebSocketConnectionsTable
        WEBSOCKET_API_ENDPOINT: !Sub "https://${WebSocketApi}.execute-api.${AWS::Region}.amazonaws.com/${Stage}"
    Policies:
      - DynamoDBCrudPolicy:
          TableName: !Ref UsersTable
      - DynamoDBCrudPolicy:
          TableName: !Ref TutorRequestsTable
      - DynamoDBCrudPolicy:
          TableName: !Ref TutorStudentsTable
      - DynamoDBCrudPolicy:
          TableName: !Ref WebSocketConnectionsTable
      - Statement:
          - Effect: Allow
            Action:
              - execute-api:ManageConnections
            Resource: !Sub "arn:aws:execute-api:${AWS::Region}:${AWS::AccountId}:${WebSocketApi}/*"
    Events:
      GetTutors:
        Type: Api
        Properties:
          Path: /api/tutors
          Method: GET
          Auth:
            Authorizer: CognitoAuthorizer
      CreateRequest:
        Type: Api
        Properties:
          Path: /api/tutors/{email}/request
          Method: POST
          Auth:
            Authorizer: CognitoAuthorizer
      GetMyRequests:
        Type: Api
        Properties:
          Path: /api/my/tutor-requests
          Method: GET
          Auth:
            Authorizer: CognitoAuthorizer
      CancelRequest:
        Type: Api
        Properties:
          Path: /api/my/tutor-requests/{id}
          Method: DELETE
          Auth:
            Authorizer: CognitoAuthorizer
      GetTutorRequests:
        Type: Api
        Properties:
          Path: /api/tutor/requests
          Method: GET
          Auth:
            Authorizer: CognitoAuthorizer
      ApproveRequest:
        Type: Api
        Properties:
          Path: /api/tutors/requests/{id}/approve
          Method: POST
          Auth:
            Authorizer: CognitoAuthorizer
      RejectRequest:
        Type: Api
        Properties:
          Path: /api/tutors/requests/{id}/reject
          Method: POST
          Auth:
            Authorizer: CognitoAuthorizer

TutorRequestsTable:
  Type: AWS::DynamoDB::Table
  Properties:
    TableName: tutor-requests
    BillingMode: PAY_PER_REQUEST
    AttributeDefinitions:
      - AttributeName: request_id
        AttributeType: S
      - AttributeName: created_at
        AttributeType: N
      - AttributeName: student_email
        AttributeType: S
      - AttributeName: tutor_email_status
        AttributeType: S
    KeySchema:
      - AttributeName: request_id
        KeyType: HASH
      - AttributeName: created_at
        KeyType: RANGE
    GlobalSecondaryIndexes:
      - IndexName: student_email-created_at-index
        KeySchema:
          - AttributeName: student_email
            KeyType: HASH
          - AttributeName: created_at
            KeyType: RANGE
        Projection:
          ProjectionType: ALL
      - IndexName: tutor_email_status-created_at-index
        KeySchema:
          - AttributeName: tutor_email_status
            KeyType: HASH
          - AttributeName: created_at
            KeyType: RANGE
        Projection:
          ProjectionType: ALL
    TimeToLiveSpecification:
      AttributeName: ttl
      Enabled: true
```

---

## 에러 처리

### 공통 에러 형식

```json
{
  "error": {
    "code": "DUPLICATE_REQUEST",
    "message": "이미 해당 튜터에게 대기 중인 요청이 있습니다.",
    "details": {
      "existing_request_id": "req_abc123",
      "status": "pending",
      "created_at": "2026-01-19T10:30:00Z"
    }
  }
}
```

### 에러 코드

| 코드 | HTTP Status | 설명 |
|------|-------------|------|
| DUPLICATE_REQUEST | 400 | 이미 대기 중인 요청 존재 |
| ALREADY_REGISTERED | 400 | 이미 해당 튜터의 학생 |
| TUTOR_NOT_ACCEPTING | 400 | 튜터가 학생을 받지 않음 |
| CAPACITY_FULL | 400 | 튜터 정원 초과 |
| TUTOR_NOT_FOUND | 404 | 튜터를 찾을 수 없음 |
| REQUEST_NOT_FOUND | 404 | 요청을 찾을 수 없음 |
| REQUEST_ALREADY_PROCESSED | 400 | 이미 처리된 요청 |
| UNAUTHORIZED | 403 | 권한 없음 |
| CANNOT_CANCEL | 400 | 취소할 수 없는 상태 |

---

## 테스트 시나리오

### 1. 정상 플로우: 승인

```
1. [학생] POST /api/tutors/tutor1@example.com/request
   → 201 Created, request_id 반환
   
2. [Lambda] 
   → tutor_requests 테이블에 status='pending' 저장
   → WebSocket: 튜터에게 NEW_TUTOR_REQUEST 알림

3. [튜터] GET /api/tutor/requests
   → 200 OK, pending 요청 1건 포함

4. [튜터] POST /api/tutors/requests/{id}/approve
   → 200 OK, 승인 완료
   
5. [Lambda]
   → tutor_requests → status='approved' 업데이트
   → tutor_students 테이블에 관계 추가 (트랜잭션)
   → WebSocket: 학생에게 TUTOR_REQUEST_APPROVED 알림

6. [학생] GET /api/my/tutor-requests
   → 200 OK, status: 'approved' 확인
```

### 2. 정상 플로우: 거부

```
1. [학생] POST /api/tutors/tutor1@example.com/request
   → 201 Created
   
2. [Lambda]
   → tutor_requests 테이블에 status='pending' 저장
   → WebSocket: 튜터에게 NEW_TUTOR_REQUEST 알림

3. [튜터] POST /api/tutors/requests/{id}/reject
   Body: { "reason": "정원 초과" }
   → 200 OK
   
4. [Lambda]
   → tutor_requests → status='rejected' 업데이트
   → rejection_reason 저장
   → WebSocket: 학생에게 TUTOR_REQUEST_REJECTED 알림

5. [학생] GET /api/my/tutor-requests
   → 200 OK, status: 'rejected', rejection_reason 확인
```

### 3. 에러 케이스: 중복 요청

```
1. [학생] POST /api/tutors/tutor1@example.com/request
   → 201 Created

2. [학생] POST /api/tutors/tutor1@example.com/request (다시 요청)
   → 400 Bad Request
   → { "error": { "code": "DUPLICATE_REQUEST", ... } }
```

### 4. 에러 케이스: 정원 초과

```
1. [학생] POST /api/tutors/tutor_full@example.com/request
   (이 튜터는 이미 정원이 가득 참)
   → 400 Bad Request
   → { "error": { "code": "CAPACITY_FULL", ... } }
```

### 5. 요청 취소

```
1. [학생] POST /api/tutors/tutor1@example.com/request
   → 201 Created, request_id: req_abc123

2. [학생] DELETE /api/my/tutor-requests/req_abc123
   → 200 OK, status: 'cancelled'

3. [튜터] GET /api/tutor/requests
   → 200 OK, 해당 요청은 목록에 없음 (cancelled는 제외)
```

---

## UI 화면 구성

### 학생 화면

#### 1. 튜터 검색

```
┌─────────────────────────────────────────┐
│  🔍 튜터 검색                            │
│  ┌─────────────────────────────────┐   │
│  │ 이름 또는 전문분야 검색...       │   │
│  └─────────────────────────────────┘   │
│                                         │
│  필터: [전체] [발음] [문법] [회화]       │
├─────────────────────────────────────────┤
│  📋 튜터 목록 (12명)                     │
├─────────────────────────────────────────┤
│  👤 이영어                              │
│     10년 경력 영어 발음 전문 튜터        │
│     전문분야: 발음, 회화                 │
│     학생: 8/15명                        │
│     [등록 요청]                          │
├─────────────────────────────────────────┤
│  👤 박발음                              │
│     미국 거주 10년 원어민 수준 발음 교정  │
│     전문분야: 발음                      │
│     학생: 10/10명 (마감)                │
│     [대기 중] ← 요청 보낸 상태           │
├─────────────────────────────────────────┤
│  👤 최회화                              │
│     영어 회화 중심 학습                  │
│     전문분야: 회화                      │
│     학생: 5/20명                        │
│     ✅ 등록됨                            │
└─────────────────────────────────────────┘
```

#### 2. 내 튜터 요청 현황

```
┌─────────────────────────────────────────┐
│  📬 내 튜터 등록 요청                    │
├─────────────────────────────────────────┤
│  ⏳ 대기 중 (1건)                        │
│                                         │
│  👤 이영어 튜터                          │
│     요청일: 2026-01-20 (1일 전)         │
│     메시지: "발음 교정을 받고 싶어서..." │
│     [요청 취소]                          │
├─────────────────────────────────────────┤
│  ✅ 승인됨 (1건)                         │
│                                         │
│  👤 최회화 튜터                          │
│     승인일: 2026-01-18                  │
│     학습 시작 →                          │
├─────────────────────────────────────────┤
│  ❌ 거부됨 (1건)                         │
│                                         │
│  👤 박발음 튜터                          │
│     거부일: 2026-01-17                  │
│     사유: "현재 정원이 가득 차서..."     │
└─────────────────────────────────────────┘
```

### 튜터 화면

#### 1. 대기 중인 등록 요청

```
┌─────────────────────────────────────────┐
│  📬 대기 중인 등록 요청 (3건)            │
├─────────────────────────────────────────┤
│  👤 김영희                              │
│     요청일: 2026-01-20 (1일 전)         │
│     메시지: "발음 교정을 받고 싶어서     │
│              등록 요청드립니다."         │
│     [승인] [거부]                        │
├─────────────────────────────────────────┤
│  👤 박철수                              │
│     요청일: 2026-01-19 (2일 전)         │
│     메시지: "회화 실력 향상을 위해       │
│              등록 요청합니다."           │
│     [승인] [거부]                        │
├─────────────────────────────────────────┤
│  👤 이순신                              │
│     요청일: 2026-01-18 (3일 전)         │
│     메시지: "잘 부탁드립니다!"          │
│     [승인] [거부]                        │
└─────────────────────────────────────────┘
```

#### 2. 거부 모달

```
┌─────────────────────────────────────────┐
│  ❌ 등록 요청 거부                       │
├─────────────────────────────────────────┤
│  학생: 김영희                            │
│                                         │
│  거부 사유 (선택사항):                   │
│  ┌─────────────────────────────────┐   │
│  │ 현재 정원이 가득 차서 더 이상    │   │
│  │ 학생을 받을 수 없습니다.         │   │
│  └─────────────────────────────────┘   │
│                                         │
│     [취소]  [거부 확정]                  │
└─────────────────────────────────────────┘
```

---

## 예상 소요 시간

| Phase | 작업 | 예상 시간 |
|-------|------|-----------|
| 1 | 인프라 구축 | 1-2일 |
| 2 | 학생 기능 | 2-3일 |
| 3 | 튜터 기능 | 2-3일 |
| 4 | WebSocket 알림 | 2일 |
| 5 | API Gateway 연동 | 1일 |
| 6 | 테스트 | 2일 |
| 7 | 추가 기능 (선택) | 1-2일 |
| **총계** | | **11-15일** |

---

## 체크리스트

### 필수 기능
- [ ] 튜터 목록 조회
- [ ] 등록 요청 생성
- [ ] 내 요청 조회
- [ ] 요청 승인
- [ ] 요청 거부
- [ ] WebSocket 실시간 알림
- [ ] 중복 요청 방지
- [ ] 정원 확인

### 선택 기능
- [ ] 요청 취소
- [ ] 검색 및 필터링
- [ ] 자동 만료
- [ ] 이메일 알림
- [ ] 튜터 평점
- [ ] 통계

### 테스트
- [ ] 단위 테스트
- [ ] 통합 테스트
- [ ] 시나리오 테스트
- [ ] 에러 케이스 테스트

---

## 참고 사항

1. **DynamoDB 트랜잭션**: 승인 시 tutor_requests 업데이트와 tutor_students 추가를 원자적으로 처리
2. **WebSocket 연결 관리**: 사용자가 오프라인이면 알림을 저장 후 다음 로그인 시 표시
3. **보안**: Cognito를 통한 인증 및 Lambda에서 사용자 role 검증
4. **성능**: GSI를 적절히 활용하여 쿼리 성능 최적화
5. **확장성**: 튜터 추천 알고리즘, 자동 매칭 등 향후 확장 가능

---

## 다음 단계

1. 이 로드맵을 팀과 공유 및 검토
2. Phase 1부터 순차적으로 구현 시작
3. 각 Phase 완료 시 테스트 및 코드 리뷰
4. 프론트엔드 팀과 API 명세 공유
5. 점진적 배포 (개발 → 스테이징 → 프로덕션)
